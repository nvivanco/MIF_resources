process {
    // Default resources (For simple tasks like file renaming or metadata)
    cpus   = { check_max( 1    * task.attempt, 'cpus'   ) }
    memory = { check_max( 6.GB * task.attempt, 'memory' ) }
    time   = { check_max( 4.h  * task.attempt, 'time'   ) }

    errorStrategy = { task.exitStatus in [1, 137, 140, 143] ? 'retry' : 'finish' }
    maxRetries = 2

    // Low-weight tasks (like OME-Zarr metadata extraction)
    withLabel: 'process_low' {
        cpus   = { check_max( 2     * task.attempt, 'cpus'   ) }
        memory = { check_max( 12.GB * task.attempt, 'memory' ) }
        time   = { check_max( 4.h   * task.attempt, 'time'   ) }
    }

    // Medium-weight (standard 2D Cellpose or small Ilastik)
    withLabel: 'process_medium' {
        cpus   = { check_max( 6     * task.attempt, 'cpus'   ) }
        memory = { check_max( 36.GB * task.attempt, 'memory' ) }
        time   = { check_max( 8.h   * task.attempt, 'time'   ) }
    }

    // Heavy-weight (3D Cellpose, Trackastra, or large Zarr re-chunking)
    withLabel: 'process_high' {
        cpus   = { check_max( 32    * task.attempt, 'cpus'   ) }
        memory = { check_max( 180.GB * task.attempt, 'memory' ) }
        time   = { check_max( 16.h  * task.attempt, 'time'   ) }
    }

    // Extreme memory (FIJI Trackmate stitching or huge volume merging)
    withLabel: 'process_high_memory' {
        cpus   = { check_max( 12     * task.attempt, 'cpus'   ) }
        memory = { check_max( 200.GB * task.attempt, 'memory' ) }
        time   = { check_max( 20.h   * task.attempt, 'time'   ) }
    }

}

