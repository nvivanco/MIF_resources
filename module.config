process {
    withName: 'ZARR_FEATURE_EXTRACTION' {
        publishDir = [
            path: { "${params.outdir}/measurements/${meta.exp_name}" },
            mode: 'copy',
            pattern: "${meta.id}.features.parquet"
        ]
    }
}
